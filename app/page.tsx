/*
 * Placeholder only. The home page is out of scope until the foundation is
 * approved; this exists so the boilerplate does not ship. Do not build the
 * counter, the gap module, or the ledger here yet.
 */
export default function Home() {
  return (
    <main className="mx-auto flex w-full max-w-4xl flex-1 flex-col justify-center px-6 py-16">
      <h1 className="font-display text-6xl text-ink">Burn Rate</h1>
      <p className="mt-3 font-sans text-base text-secondary">
        Space is the new frontier of human investment. Money is propellant, and it burns
        first.
      </p>
      <p className="mt-8 font-mono text-xs uppercase tracking-widest text-muted">
        Under construction · <a href="/styleguide">styleguide</a>
      </p>
    </main>
  );
}
