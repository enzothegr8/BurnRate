// This repository is public and every headline, figure, and module in this
// build is invented. A publication whose premise is marking its numbers cannot
// have a deployed page that reads as published work when it is not, so the
// page says so at the top rather than in a comment nobody sees.
export function PlaceholderBar() {
  return (
    <div className="ph-bar">
      Placeholder build ·{" "}
      <b>every headline, figure, and module below is placeholder</b> · structure
      only
    </div>
  );
}
